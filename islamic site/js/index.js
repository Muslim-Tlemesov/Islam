/**
 * js/index.js
 * Главная страница пока полностью статична — она просто ведёт в раздел
 * «Хадисы». Файл оставлен как точка расширения (например, для будущего
 * блока с последними добавленными хадисами), чтобы не создавать
 * отдельный HTML при появлении такой логики.
 */

document.addEventListener("DOMContentLoaded", () => {
  // Точка расширения на будущее.
});
